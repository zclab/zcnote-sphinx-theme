================
基础使用教程
================

当主题安装完成后，你需要在 ``conf.py`` 中进行如下配置。

启用主题
========

.. code-block:: python

   html_theme = 'zcnote_sphinx_theme'

自定义配置项
============

目前主题支持以下自定义配置：

* ``enable_collapse_sidebar``: 控制是否显示侧边栏折叠按钮。
