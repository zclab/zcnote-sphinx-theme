/**
 * ZC-Note Sphinx Theme 核心脚本入口
 */
import { initSidebar } from './sidebar.js';
import { initFooter } from './footer.js';

// =========================================================
// 1. 侧边栏逻辑：立即执行 (防闪烁)
// =========================================================
// 因为我们在 Python 端注入脚本时使用了 `defer="defer"` 属性，
// 此时 DOM 树已经解析完成但尚未绘制到屏幕。
// 此时直接执行 DOM 转移操作，能完美消除折叠按钮的瞬间跳动。
initSidebar();

// =========================================================
// 2. 底部栏逻辑：等待 DOM 完全就绪
// =========================================================
// Footer 的零跳动计算高度依赖完整的页面布局、图片尺寸和外围容器。
// 放在 DOMContentLoaded 中执行最为稳妥。
document.addEventListener("DOMContentLoaded", () => {
    initFooter();
});
